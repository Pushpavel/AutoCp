#**
 * Rust Competitive Programming Template
 *
 * wrap the code like #[[ code ]]# so that it is generated as it is
 *#
#[[ use std::error::Error;
use std::fs::File;

// Anything that can give exception gets a ? after it.
fn main() {
    
}
]]#